# Base URL of the NestJS API (server/), without the /api/v1 suffix -- api-client.ts appends it.
# Safe to expose to the browser: it is a public origin, never a secret (SEC-016/FE-011).
NEXT_PUBLIC_API_URL=http://localhost:3001
